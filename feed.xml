<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://davidfobar.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://davidfobar.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-20T18:41:13+00:00</updated><id>https://davidfobar.github.io/feed.xml</id><title type="html">Platypus Worx</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Casting a Full-Diameter Lap Tool Out of Dental Stone</title><link href="https://davidfobar.github.io/blog/2026/dental-stone-tool/" rel="alternate" type="text/html" title="Casting a Full-Diameter Lap Tool Out of Dental Stone"/><published>2026-09-20T10:00:00+00:00</published><updated>2026-09-20T10:00:00+00:00</updated><id>https://davidfobar.github.io/blog/2026/dental-stone-tool</id><content type="html" xml:base="https://davidfobar.github.io/blog/2026/dental-stone-tool/"><![CDATA[<p>Working a mirror takes two discs, not one. The mirror is the piece you keep; the <strong>tool</strong> is the other half of the sandwich, the thing the abrasive or the polishing compound works between. For the <a href="/projects/2_atm_6in_f5/">6” f/5</a> I cast a full-diameter tool out of dental stone — directly against the mirror it’s going to work.</p> <p>This one is specifically a <strong>lap tool</strong>. It gets a pitch lap poured onto it and goes to polishing; it is not what I’d use for rough or fine grinding. That distinction turns out to drive two of the decisions below.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/dental-stone-tool/DS_scored_face.JPEG" sizes="95vw"/> <img src="/assets/img/posts/dental-stone-tool/DS_scored_face.JPEG" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">The finished lap tool, working face up, scored with a utility knife to key the pitch.</figcaption> </figure> </div> </div> <h2 id="why-cast-a-tool-at-all">Why cast a tool at all</h2> <p>The obvious move is to buy a second glass blank and grind the two against each other, which is how mirrors were made for a century and works fine. It’s also twice the glass and a lot of hogging out material you’ll throw away.</p> <p>Casting sidesteps the whole thing. Dental stone is cheap, it pours into any shape you can build a dam around, and — the part that matters here — it takes the curve you cast it against <strong>exactly</strong>, with no grinding required to get there. You skip straight to the tool you would have spent hours producing.</p> <p>What goes <em>into</em> the casting depends on the job. A tool meant for rough or fine grinding gets ceramic tiles embedded in the face as it’s poured: the tiles do the cutting, because they’re hard enough to abrade glass for hours and the stone around them is not. A tool meant to carry a pitch lap doesn’t need tiles, because it never touches the glass — the pitch does. This one is bare stone for exactly that reason, and the difference shows up again in how the face gets finished.</p> <p>The specific product is <strong>Velmix</strong>, a Type IV dental die stone. Dentists use it for working models that have to hold fine detail and survive handling, which is nearly the same spec sheet a mirror tool wants: fine particle size, high compressive strength, and low setting expansion. That last one is the important property. A stone that swells appreciably as it sets gives you a tool that no longer matches the curve you cast it on, and the whole reason to cast is that the curve comes out right.</p> <h2 id="the-19-millimetres">The 1.9 millimetres</h2> <p>Here’s the number that rules out the lazy option — casting the tool flat on a bench and calling it close enough.</p> <p>A 6” f/5 mirror has a focal length of 762 mm and a radius of curvature of <strong>R = 1524 mm</strong>. The depth of the curve at the centre, the sagitta, is</p> \[s \approx \frac{r^2}{2R} = \frac{(76.2\ \text{mm})^2}{2 \times 1524\ \text{mm}} = 1.9\ \text{mm}\] <p>So the mirror’s centre sits 1.9 mm deeper than its edge. That sounds like nothing. It isn’t. A pitch lap is a <em>contact</em> instrument — it polishes where it touches, and it removes material measured in fractions of a micron per session. Lay a flat lap on a 1.9 mm dish and it rides on the outer edge, polishing a narrow annulus and never reaching the centre at all. The pitch will eventually cold-flow to fit, but you’d be spending hours of press time getting the lap to the shape you could have simply cast it at.</p> <p>Cast against the mirror, the tool starts at zero mismatch. That’s the entire argument.</p> <h2 id="the-mould-is-the-mirror">The mould is the mirror</h2> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/dental-stone-tool/DS_mold_ready.JPEG" sizes="95vw"/> <img src="/assets/img/posts/dental-stone-tool/DS_mold_ready.JPEG" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">The dam taped up around the mirror. The mirror is face-up underneath, covered in plastic film held down with radial tape strips.</figcaption> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/dental-stone-tool/DS_fresh_pour.JPEG" sizes="95vw"/> <img src="/assets/img/posts/dental-stone-tool/DS_fresh_pour.JPEG" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Fresh pour. Velmix goes in about the consistency of heavy cream and self-levels.</figcaption> </figure> </div> </div> <p>The setup is about as simple as it gets: the mirror face-up on the bench, a layer of plastic film over it as a release layer, and a dam wrapped around it and taped.</p> <p>The dam is <strong>cake collar</strong> — the clear acetate strip used for lining cake tins. It’s the right material almost by accident: it’s stiff enough to hold a circle, thin enough to wrap a tight radius, cheap by the roll, and transparent, so you can watch the pour fill from the side and see whether air is trapped underneath instead of guessing.</p> <p>I used 4” collar. I’d use 2” next time. The tool only needs to be an inch or so thick, so most of that height was just wall above the pour doing nothing except making the thing awkward to tape and easy to bow. Buy the narrow roll.</p> <p>One other detail earns its keep: the film gets pulled down and taped radially so it lies against the glass rather than bridging across the dish. Any wrinkle that bridges is a void in the tool.</p> <p>The film does cost you a little fidelity. You’re casting against plastic-wrapped glass, not bare glass, so the tool inherits the film’s thickness and whatever texture it has. Here that’s irrelevant: a pitch lap is millimetres of pitch thick and conforms under pressure anyway, so a few thousandths of an inch of film in the substrate disappears entirely. It would matter if you were trying to cast an optical surface, which you are not.</p> <p>Velmix mixes at a low water-to-powder ratio, which is what buys the strength, and it pours thick. Mix it too wet to make pouring easier and you trade away exactly the property you picked this material for.</p> <h2 id="out-of-the-dam">Out of the dam</h2> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/dental-stone-tool/DS_cured_in_dam.JPEG" sizes="95vw"/> <img src="/assets/img/posts/dental-stone-tool/DS_cured_in_dam.JPEG" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Cured and still in the dam, sitting on the grinding stand's plate.</figcaption> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/dental-stone-tool/DS_edge_cleanup.JPEG" sizes="95vw"/> <img src="/assets/img/posts/dental-stone-tool/DS_edge_cleanup.JPEG" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Knocking the edge back with a file. Die stone cuts easily and makes a lot of very fine dust.</figcaption> </figure> </div> </div> <p>Peeling the dam off leaves a clean cylinder with a ragged rim — the meniscus where the stone climbed the plastic, plus a thin flash where it crept under. A file takes care of both in a few minutes, and the edge gets a bevel while the file is out.</p> <p>That bevel is not cosmetic. An unbevelled stone edge chips, and a chip off the tool edge becomes a hard fragment loose in the abrasive, which is how you put a scratch into a mirror you’ve already spent hours on. Bevel everything, always, on both the tool and the mirror.</p> <p>Die stone cuts fast and throws a huge amount of very fine dust, so this is a mask-and-outside job. Both of these photos are on the <a href="/projects/11_grinding_stand/">concrete grinding stand</a> — the pink dust is all over the index marks on the grinding plate.</p> <h2 id="scoring-the-face">Scoring the face</h2> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/dental-stone-tool/DS_scored_face.JPEG" sizes="95vw"/> <img src="/assets/img/posts/dental-stone-tool/DS_scored_face.JPEG" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Random crosshatch, cut freehand with a utility knife — a mechanical key for the pitch. No grid, on purpose.</figcaption> </figure> </div> </div> <p>The last step is to cut a crosshatch of grooves across the working face with a utility knife. These are there for one reason: <strong>to give the pitch something to grab.</strong></p> <p>Pitch poured onto a smooth stone face is held by not much more than surface adhesion, and polishing is hours of exactly the shear load that finds that out. Cut grooves into the stone first and the pitch flows into them as it’s poured, so once it sets the lap is keyed to the substrate mechanically rather than just stuck to it. The grooves are undercut by the knife’s V-profile, which helps.</p> <p>The pattern is deliberately random rather than a neat grid. A regular grid has a period, and anything with a period in mirror work is a chance to print that period into the glass. Random cuts have no preferred direction and no spacing to transfer.</p> <p>Worth being clear about what this scoring is <em>not</em>: it isn’t channelling for grinding slurry. A grinding tool solves that problem differently, with the gaps between its embedded tiles.</p> <h2 id="next-pitch">Next: pitch</h2> <p>The tool is done and waiting on the thing it exists for: a pitch lap poured onto this face. That’s the surface that will do the polishing and, eventually, the figuring that the <a href="/blog/2026/foucault-test-explained/">Foucault test</a> is there to measure.</p> <p>That’s the next post.</p>]]></content><author><name></name></author><category term="atm-6in-f5"/><category term="optics"/><category term="telescopes"/><category term="atm"/><summary type="html"><![CDATA[A full-diameter lap tool for the 6" mirror, cast in Velmix die stone straight off the mirror's own face. Why you cast instead of buy, the 1.9 mm that rules out a flat tool, and why this one has no tiles in it.]]></summary></entry><entry><title type="html">The Foucault Test, With Actual Numbers</title><link href="https://davidfobar.github.io/blog/2026/foucault-test-explained/" rel="alternate" type="text/html" title="The Foucault Test, With Actual Numbers"/><published>2026-09-16T12:00:00+00:00</published><updated>2026-09-16T12:00:00+00:00</updated><id>https://davidfobar.github.io/blog/2026/foucault-test-explained</id><content type="html" xml:base="https://davidfobar.github.io/blog/2026/foucault-test-explained/"><![CDATA[<p>The entire job of parabolizing a 6-inch f/5 mirror — the difference between a sphere that shows you a fuzzy blob and a paraboloid that splits a double star — lives inside <strong>1.9 millimetres of knife-edge travel</strong>.</p> <p>That number is the reason this post exists. Every explanation of the Foucault test I could find falls into one of two camps: a qualitative hand-wave with a photo of a donut-shaped shadow, or a dense zonal-measurement table that assumes you already know what you’re doing. The part in the middle — <em>how far does the thing actually move, and why</em> — is almost always left out, buried in a dial indicator reading that nobody bothers to explain.</p> <p>This is my attempt at the missing middle. It’s a companion to <a href="/projects/3_automated_foucault/">Automated Foucault</a>, where I’m replacing the eyeball and the dial indicator with a camera and a motorised stage. The inspiration is a battered copy of <em>Understanding Foucault</em>, which is the book that finally made this click for me — this post is the interactive version of what that book does with diagrams.</p> <p>Everything below is traced with the exact law of reflection off a real paraboloid. No paraxial approximation, no fudge factors. The pictures are not to scale; <strong>the numbers are.</strong></p> <h2 id="the-apparatus">The apparatus</h2> <p>A Foucault tester is almost insultingly simple. You need three things, and they all ride on one stage that slides along the optical axis:</p> <ul> <li><strong>A pinhole light source</strong> — a bright LED behind a small hole or a narrow slit.</li> <li><strong>A knife edge</strong> — literally a razor blade, or the edge of anything flat and opaque.</li> <li><strong>Something to look with</strong> — your eye, historically. A camera sensor, in my case.</li> </ul> <p>You put the mirror at one end of a long room, and the stage at the other end, near the mirror’s <strong>centre of curvature</strong> — twice the focal length away. For a 6” f/5, focal length 762 mm, that’s a radius of curvature of <strong>R = 1524 mm</strong>, so the stage sits about five feet from the mirror.</p> <p>The pinhole sits a couple of millimetres <em>below</em> the optical axis. Light from it travels down to the mirror, bounces, and comes back to a point a couple of millimetres <em>above</em> the axis. That flip is not a coincidence: at the centre of curvature, object and image are conjugate with magnification −1, so the returning image lands exactly as far on the other side of the axis as the source is on this side. The knife edge waits there, at the image, and slices into it.</p> <p>That’s the whole instrument. The subtlety is entirely in what happens when you slide the stage a fraction of a millimetre.</p> <h2 id="start-with-a-sphere-because-a-sphere-is-easy">Start with a sphere, because a sphere is easy</h2> <p>Grind two pieces of glass against each other with abrasive between them, and the surface you naturally get is a <strong>sphere</strong>. That’s not a coincidence either — a sphere is the only shape where every part of one surface fits every part of the other in every orientation, so random strokes converge on it.</p> <p>A sphere has a magic property for our purposes. Put a point source at its centre of curvature, and <em>every single ray</em> hits the glass dead perpendicular to the surface, reflects straight back along the path it came, and returns to exactly the point it started from. Not approximately. Exactly. The edge of the mirror and the centre of the mirror send their light back to the same place.</p> <p>So when you slide the knife edge into that returning cone, it cuts <strong>every zone of the mirror at the same instant</strong>. The whole disk goes from lit to dark together, evenly, like a dimmer switch.</p> <p>Set the simulation below to <em>Sphere</em> and it drops you straight onto that position: a flat, featureless grey disk. Now drag the stage off it in either direction. You’ll see a shadow sweep across — that’s just defocus, the blade cutting a cone that isn’t converged yet — but notice what you <em>never</em> see: a ring, a bump, a dark centre, any hint that one part of the mirror behaves differently from another. The disk stays featureless right across. Every zone agrees.</p> <p>That uniform grey-out is the most sensitive null test in optics. It’s how you know you have a sphere.</p> <h2 id="a-paraboloid-cannot-do-that">A paraboloid cannot do that</h2> <p>A telescope doesn’t want a sphere. It wants a <strong>paraboloid</strong>, because a paraboloid is the shape that brings light from infinitely far away — a star — to a single point.</p> <p>Here’s the problem: those two requirements fight each other. The shape that focuses parallel light perfectly is not the shape that focuses light from its own centre of curvature perfectly. So the moment you start parabolizing, you break the clean null, and the mirror starts showing you structure.</p> <p>How much do you actually have to change the glass? Less than you’d ever guess. The depth difference between the paraboloid you want and the sphere you started with, measured at the edge of the mirror, is</p> \[\Delta z = \frac{r^4}{8R^3}\] <p>For the 6” f/5 — edge radius \(r = 76.2\) mm, \(R = 1524\) mm — that comes out to <strong>1.19 micrometres</strong>. About two wavelengths of green light. A fifth the thickness of a red blood cell. That, and nothing more, is the total amount of glass standing between a sphere and a finished telescope mirror.</p> <p>You obviously cannot measure 1.2 µm of glass with a ruler. What makes the Foucault test extraordinary is that it doesn’t ask you to.</p> <h2 id="the-lever">The lever</h2> <p>Because the paraboloid is shallower at the edge than the matching sphere, the edge zone of the mirror is very slightly <em>less</em> curved than the middle. Less curvature means a longer radius of curvature, which means the light from the edge crosses the axis <strong>farther away from the mirror</strong> than the light from the centre does.</p> <p>The mirror no longer has <em>a</em> centre of curvature. It has a smeared-out range of them — a different one for every zone — spread along the axis. And the spread is enormous compared to the surface error that caused it.</p> <p>For a tester where the source and knife edge move together on one stage, the zone at radius \(r\) nulls when the stage sits at</p> \[x_r = R + \frac{r^2}{2R}\] <p>Run the numbers for the edge of the 6” f/5: \(76.2^2 / (2 \times 1524) = 1.905\) mm.</p> <table> <thead> <tr> <th> </th> <th>value</th> </tr> </thead> <tbody> <tr> <td>Glass you have to remove at the edge</td> <td>1.19 µm</td> </tr> <tr> <td>Knife-edge travel it produces</td> <td>1.905 mm</td> </tr> <tr> <td><strong>Amplification</strong></td> <td><strong>≈ 1600×</strong></td> </tr> </tbody> </table> <p>That is the Foucault test. It’s a lever that converts a micron of glass you cannot see into two millimetres of stage travel you can measure with a dial indicator. Every bit of the rest is bookkeeping.</p> <h2 id="try-it">Try it</h2> <p>Drag the stage and watch three things at once: where the rays actually cross, where the blade is sitting, and what your eye would see looking into the mirror.</p> <p>The middle panel is drawn to real millimetres along the axis — those tick marks are honest. The mirror face on the right is computed per-pixel from the same ray trace, not drawn by hand, which is why the shadows flip sides on their own when you pass through a crossing point.</p> <p><strong>Things worth doing:</strong> flip to <em>Sphere</em> and sweep the stage — nothing but flat grey, everywhere. Flip back to <em>Parabola</em> and sweep again. Then use the zone buttons to null the centre, the 70.7% zone, and the edge in turn, and watch the dark ring march outward while the readout counts off the millimetres.</p> <div class="fc-sim" id="fc-sim"> <div class="fc-controls"> <div class="fc-row"> <label class="fc-inline">Mirror <select id="fc-mirror"> <option value="6f5" selected="">6&quot; f/5 &mdash; R = 1524 mm</option> <option value="6f8">6&quot; f/8 &mdash; R = 2438 mm</option> <option value="8f6">8&quot; f/6 &mdash; R = 2438 mm</option> </select> </label> <span class="fc-seg" id="fc-figure"> <button type="button" data-fig="sphere">Sphere</button> <button type="button" data-fig="para" class="fc-on">Parabola</button> </span> <label class="fc-inline fc-check"><input type="checkbox" id="fc-zones"/> Zone rings</label> </div> <div class="fc-slider-row"> <label for="fc-stage">Stage position</label> <input type="range" id="fc-stage" min="0" max="1000" step="1" value="171"/> <span class="fc-val" id="fc-stage-val">+0.000 mm</span> </div> <div class="fc-slider-row"> <label for="fc-knife">Knife insertion</label> <input type="range" id="fc-knife" min="0" max="1000" step="1" value="500"/> <span class="fc-val" id="fc-knife-val">0.000 mm</span> </div> <div class="fc-presets"> <span>Null the zone at:</span> <button type="button" data-zone="0">centre</button> <button type="button" data-zone="0.5">50%</button> <button type="button" data-zone="0.707">70.7%</button> <button type="button" data-zone="0.85">85%</button> <button type="button" data-zone="1">edge</button> </div> </div> <div class="fc-panel fc-layout-panel"> <h4>1 &mdash; The layout <em>(schematic: curvature and stage travel hugely exaggerated)</em></h4> <svg viewBox="0 0 720 180" class="fc-svg" id="fc-layout"> <line x1="60" y1="90" x2="700" y2="90" class="fc-axis"></line> <path d="M 662 18 Q 700 90 662 162" class="fc-mirror"></path> <text x="676" y="176" class="fc-lbl fc-lbl-mid">mirror</text> <g id="fc-rays-layout"></g> <g id="fc-stage-g"> <rect x="86" y="46" width="7" height="88" class="fc-sensor"></rect> <text x="89" y="40" class="fc-lbl fc-lbl-mid">sensor</text> <rect x="104" y="22" width="5" height="66" class="fc-knife"></rect> <text x="122" y="30" class="fc-lbl">knife edge</text> <circle cx="106" cy="98" r="4" class="fc-pinhole"></circle> <text x="122" y="112" class="fc-lbl">pinhole source</text> <rect x="80" y="140" width="38" height="6" rx="2" class="fc-carriage"></rect> </g> <line x1="112" y1="166" x2="660" y2="166" class="fc-dim"></line> <text x="386" y="160" class="fc-lbl fc-lbl-mid" id="fc-R-label">R = 1524 mm</text> </svg> </div> <div class="fc-panels"> <div class="fc-panel"> <h4>2 &mdash; Where the rays actually cross</h4> <svg viewBox="0 0 520 210" class="fc-svg" id="fc-zoom"></svg> <p class="fc-cap" id="fc-zoom-cap"></p> </div> <div class="fc-panel"> <h4>3 &mdash; What you see in the mirror</h4> <canvas id="fc-face" width="320" height="320" class="fc-face"></canvas> <p class="fc-cap">Computed per-pixel from the ray trace &mdash; the shadow flips sides on its own.</p> </div> </div> <div class="fc-readout"> <div><span>Stage offset from paraxial centre of curvature</span><b id="fc-ro-stage">+0.000 mm</b></div> <div><span>Same, in dial-indicator units</span><b id="fc-ro-thou">0.0 thou</b></div> <div><span>Zone currently nulled</span><b id="fc-ro-zone">&mdash;</b></div> <div><span>Total centre-to-edge travel, this mirror</span><b id="fc-ro-total">1.905 mm</b></div> </div> </div> <style>
  #fc-sim {
    border: 1px solid var(--global-divider-color);
    border-radius: 12px;
    padding: 1.25rem 1.4rem 1.4rem;
    margin: 2rem 0;
    background: var(--global-card-bg-color);
  }
  #fc-sim .fc-controls { display: flex; flex-direction: column; gap: 0.65rem; margin-bottom: 1.1rem; }
  #fc-sim .fc-row { display: flex; flex-wrap: wrap; align-items: center; gap: 1rem; }
  #fc-sim .fc-inline { font-size: 0.88rem; color: var(--global-text-color); }
  #fc-sim select { margin-left: 0.4rem; padding: 0.2rem 0.35rem; font-size: 0.85rem;
    background: var(--global-bg-color); color: var(--global-text-color);
    border: 1px solid var(--global-divider-color); border-radius: 6px; }
  #fc-sim .fc-check input { margin-right: 0.3rem; vertical-align: middle; }
  #fc-sim .fc-seg { display: inline-flex; border: 1px solid var(--global-divider-color); border-radius: 8px; overflow: hidden; }
  #fc-sim .fc-seg button { border: 0; background: transparent; color: var(--global-text-color);
    padding: 0.28rem 0.85rem; font-size: 0.85rem; cursor: pointer; }
  #fc-sim .fc-seg button.fc-on { background: var(--global-theme-color); color: #fff; }
  #fc-sim .fc-slider-row { display: grid; grid-template-columns: 8.5rem 1fr 6rem; align-items: center; gap: 0.75rem; }
  #fc-sim .fc-slider-row label { font-size: 0.88rem; color: var(--global-text-color); }
  #fc-sim input[type="range"] { width: 100%; }
  #fc-sim .fc-val { font-variant-numeric: tabular-nums; text-align: right; font-size: 0.85rem; color: var(--global-text-color); }
  #fc-sim .fc-presets { display: flex; flex-wrap: wrap; align-items: center; gap: 0.4rem; font-size: 0.85rem; color: var(--global-text-color); }
  #fc-sim .fc-presets span { opacity: 0.75; margin-right: 0.15rem; }
  #fc-sim .fc-presets button, #fc-sim .fc-seg button { font-family: inherit; }
  #fc-sim .fc-presets button { padding: 0.22rem 0.6rem; font-size: 0.8rem; border-radius: 7px;
    border: 1px solid var(--global-theme-color); background: transparent; color: var(--global-theme-color); cursor: pointer; }
  #fc-sim .fc-presets button:hover { background: var(--global-theme-color); color: #fff; }
  #fc-sim .fc-panel { margin-top: 0.9rem; }
  #fc-sim .fc-panel h4 { margin: 0 0 0.4rem; font-size: 0.86rem; font-weight: 600; color: var(--global-text-color); }
  #fc-sim .fc-panel h4 em { font-weight: 400; opacity: 0.6; font-size: 0.8rem; }
  #fc-sim .fc-panels { display: flex; gap: 1.2rem; flex-wrap: wrap; align-items: flex-start; }
  #fc-sim .fc-panels > .fc-panel:first-child { flex: 2 1 330px; min-width: 300px; }
  #fc-sim .fc-panels > .fc-panel:last-child { flex: 1 1 210px; min-width: 200px; max-width: 300px; }
  #fc-sim .fc-svg { width: 100%; height: auto; display: block; }
  #fc-sim .fc-face { width: 100%; height: auto; display: block; border-radius: 50%; background: #0b0b0b; }
  #fc-sim .fc-cap { font-size: 0.75rem; opacity: 0.7; margin: 0.4rem 0 0; line-height: 1.45; }
  #fc-sim .fc-axis { stroke: var(--global-divider-color); stroke-width: 1; stroke-dasharray: 5 4; }
  #fc-sim .fc-mirror { fill: none; stroke: var(--global-theme-color); stroke-width: 4; stroke-linecap: round; }
  #fc-sim .fc-ray { fill: none; stroke: #e0a92b; stroke-width: 1; opacity: 0.75; }
  #fc-sim .fc-ray-zoom { stroke: #e0a92b; stroke-width: 1.2; opacity: 0.85; }
  #fc-sim .fc-knife, #fc-sim .fc-knife-zoom { fill: var(--global-text-color); opacity: 0.85; }
  #fc-sim .fc-sensor { fill: var(--global-divider-color); }
  #fc-sim .fc-carriage { fill: var(--global-theme-color); opacity: 0.55; }
  #fc-sim .fc-pinhole { fill: #e0a92b; stroke: #e0a92b; stroke-width: 5; stroke-opacity: 0.25; }
  #fc-sim .fc-lbl { font-size: 9px; fill: var(--global-text-color); opacity: 0.7; }
  #fc-sim .fc-lbl-mid { text-anchor: middle; }
  #fc-sim .fc-dim { stroke: var(--global-text-color); stroke-width: 0.75; opacity: 0.35; }
  #fc-sim .fc-tick { stroke: var(--global-text-color); stroke-width: 0.75; opacity: 0.45; }
  #fc-sim .fc-cross { fill: var(--global-theme-color); }
  #fc-sim .fc-readout { display: flex; flex-wrap: wrap; gap: 1.4rem; margin-top: 1.1rem;
    padding-top: 0.9rem; border-top: 1px solid var(--global-divider-color); }
  #fc-sim .fc-readout div { display: flex; flex-direction: column; gap: 0.15rem; }
  #fc-sim .fc-readout span { font-size: 0.72rem; opacity: 0.65; color: var(--global-text-color); }
  #fc-sim .fc-readout b { font-variant-numeric: tabular-nums; font-size: 1rem; color: var(--global-text-color); }
  @media (max-width: 600px) {
    #fc-sim { padding: 1rem; }
    #fc-sim .fc-slider-row { grid-template-columns: 5.5rem 1fr 5rem; gap: 0.5rem; }
    #fc-sim .fc-panels > .fc-panel:last-child { max-width: none; }
  }
</style> <script>
(function () {
  var root = document.getElementById('fc-sim');
  if (!root) return;

  /* ---------- mirrors ---------- */
  var MIRRORS = {
    '6f5': { D: 152.4, f: 762.0 },
    '6f8': { D: 152.4, f: 1219.2 },
    '8f6': { D: 203.2, f: 1219.2 }
  };

  var state = { key: '6f5', figure: 'para', stage: 0, knife: 0, zones: false };
  var geom = null;

  /* ---------- exact ray trace ---------- */
  // Surface sag and slope at height rr, for a paraboloid or a sphere of
  // vertex radius of curvature R. Mirror vertex at z = 0, opening toward +z.
  function surface(rr, R, figure) {
    if (figure === 'sphere') {
      var s = Math.sqrt(R * R - rr * rr);
      return [R - s, rr / s];
    }
    return [rr * rr / (2 * R), rr / R];
  }

  // On-axis point source at z = xStage. Reflect off the surface at height rr
  // using the exact law of reflection, then report where the returning ray
  // crosses the axis and how high it is back at the knife-edge plane.
  function trace(rr, R, figure, xStage) {
    var sp = surface(rr, R, figure);
    var Pz = sp[0];
    var nx = -sp[1], nz = 1.0;
    var nn = Math.sqrt(nx * nx + nz * nz); nx /= nn; nz /= nn;
    var dx = rr, dz = Pz - xStage;
    var dd = Math.sqrt(dx * dx + dz * dz); dx /= dd; dz /= dd;
    var dot = dx * nx + dz * nz;
    var ax = dx - 2 * dot * nx, az = dz - 2 * dot * nz;
    return {
      zCross: Pz - rr * az / ax,            // axial crossing of the return ray
      y: rr + ((xStage - Pz) / az) * ax,    // height at the knife-edge plane
      slope: ax / az
    };
  }

  /* ---------- per-frame model ---------- */
  var NZ = 240;
  function build() {
    var m = MIRRORS[state.key];
    var R = 2 * m.f, rEdge = m.D / 2;
    var xStage = R + state.stage;
    var ys = new Float64Array(NZ + 1), zc = new Float64Array(NZ + 1), sl = new Float64Array(NZ + 1);
    for (var i = 1; i <= NZ; i++) {
      var t = trace(rEdge * i / NZ, R, state.figure, xStage);
      ys[i] = t.y; zc[i] = t.zCross; sl[i] = t.slope;
    }
    // r = 0 is a limiting case the trace can't take directly: the paraxial ray
    // has no height to reflect at. Use the mirror equation for its crossing.
    ys[0] = 0; sl[0] = 0;
    zc[0] = 1 / (2 / R - 1 / xStage);
    return { R: R, rEdge: rEdge, xStage: xStage, ys: ys, zc: zc, sl: sl };
  }

  function setMirror(key) {
    state.key = key;
    var m = MIRRORS[key];
    var R = 2 * m.f, rEdge = m.D / 2;
    var edgeNull = rEdge * rEdge / (2 * R);
    geom = {
      R: R, rEdge: rEdge, edgeNull: edgeNull,
      stageMin: -0.30 * edgeNull,
      stageMax: 1.45 * edgeNull,
      yScale: (rEdge / R) * edgeNull * 2,          // typical |y| at the knife plane
      soft: (rEdge / R) * edgeNull * 2 * 0.06      // finite source size, in mm
    };
    // With a moving source the crossing point retreats at twice the stage rate,
    // so the window has to reach out to 2*edgeNull beyond the furthest blade.
    geom.zmin = R + geom.stageMin;
    geom.zmax = R + 2 * edgeNull - geom.stageMin;
    geom.zHalf = geom.yScale * 1.4;
    if (state.stage < geom.stageMin) state.stage = geom.stageMin;
    if (state.stage > geom.stageMax) state.stage = geom.stageMax;
    if (Math.abs(state.knife) > geom.yScale) state.knife = 0;
  }

  /* ---------- sliders map 0..1000 onto real millimetres ---------- */
  var stageEl = root.querySelector('#fc-stage');
  var knifeEl = root.querySelector('#fc-knife');

  function stageToSlider() {
    return Math.round(1000 * (state.stage - geom.stageMin) / (geom.stageMax - geom.stageMin));
  }
  function sliderToStage(v) {
    return geom.stageMin + (v / 1000) * (geom.stageMax - geom.stageMin);
  }
  function knifeToSlider() { return Math.round(500 + 500 * state.knife / geom.yScale); }
  function sliderToKnife(v) { return ((v - 500) / 500) * geom.yScale; }

  /* ---------- panel 1: layout ---------- */
  var layoutRays = root.querySelector('#fc-rays-layout');
  var stageG = root.querySelector('#fc-stage-g');
  var rLabel = root.querySelector('#fc-R-label');
  var LAYOUT_PX_PER_MM = 18;   // stage travel, hugely exaggerated so it is visible

  for (var k = 0; k < 5; k++) {
    var p = document.createElementNS('http://www.w3.org/2000/svg', 'polyline');
    p.setAttribute('class', 'fc-ray');
    layoutRays.appendChild(p);
  }
  var layoutHeights = [25, 57, 90, 123, 155];

  function drawLayout() {
    var dx = (state.stage - geom.stageMin) * LAYOUT_PX_PER_MM;
    stageG.setAttribute('transform', 'translate(' + (-dx).toFixed(2) + ' 0)');
    var srcX = 106 - dx, srcY = 98, imgY = 82;
    var nodes = layoutRays.childNodes;
    for (var i = 0; i < 5; i++) {
      var mx = 660, my = layoutHeights[i];
      nodes[i].setAttribute('points',
        srcX.toFixed(1) + ',' + srcY + ' ' + mx + ',' + my + ' ' + srcX.toFixed(1) + ',' + imgY);
    }
    rLabel.textContent = 'R = ' + geom.R.toFixed(0) + ' mm';
  }

  /* ---------- panel 2: the crossing region ---------- */
  var zoomSvg = root.querySelector('#fc-zoom');
  var zoomCap = root.querySelector('#fc-zoom-cap');
  var ZX0 = 58, ZX1 = 500, ZY = 100, ZYSPAN = 74;
  var ZONES = [1.0, 0.85, 0.707, 0.5, 0.3];
  var zoomParts = null;

  function svgEl(tag, attrs) {
    var e = document.createElementNS('http://www.w3.org/2000/svg', tag);
    for (var a in attrs) e.setAttribute(a, attrs[a]);
    return e;
  }

  function initZoom() {
    zoomSvg.innerHTML = '';
    var ticks = svgEl('g', {});
    var rays = svgEl('g', {});
    var dots = svgEl('g', {});
    zoomSvg.appendChild(svgEl('line', { x1: ZX0, y1: ZY, x2: ZX1, y2: ZY, 'class': 'fc-axis' }));
    zoomSvg.appendChild(ticks);
    zoomSvg.appendChild(rays);
    zoomSvg.appendChild(dots);
    var blade = svgEl('rect', { 'class': 'fc-knife-zoom', width: 4, x: 0, y: 0, height: 0 });
    zoomSvg.appendChild(blade);
    var rayEls = [], dotEls = [];
    for (var i = 0; i < ZONES.length; i++) {
      for (var s = 0; s < 2; s++) {
        var l = svgEl('line', { 'class': 'fc-ray-zoom' });
        rays.appendChild(l); rayEls.push(l);
      }
      var d = svgEl('circle', { r: 2.6, 'class': 'fc-cross' });
      dots.appendChild(d); dotEls.push(d);
    }
    zoomParts = { ticks: ticks, rays: rayEls, dots: dotEls, blade: blade };
  }

  function drawZoom(tab) {
    var zmin = geom.zmin, zmax = geom.zmax;
    var pxPerMm = (ZX1 - ZX0) / (zmax - zmin);
    var pxPerMmY = ZYSPAN / geom.zHalf;
    var Z = function (z) { return ZX0 + (z - zmin) * pxPerMm; };
    var Y = function (y) { return ZY - y * pxPerMmY; };

    // axis ticks every 0.5 mm from the paraxial centre of curvature
    zoomParts.ticks.innerHTML = '';
    for (var t = Math.ceil((zmin - geom.R) / 0.5) * 0.5; t <= zmax - geom.R + 1e-9; t += 0.5) {
      var px = Z(geom.R + t);
      zoomParts.ticks.appendChild(svgEl('line', { x1: px, y1: ZY - 4, x2: px, y2: ZY + 4, 'class': 'fc-tick' }));
      var lab = svgEl('text', { x: px, y: ZY + 17, 'class': 'fc-lbl fc-lbl-mid' });
      lab.textContent = (t >= 0 ? '+' : '') + t.toFixed(1);
      zoomParts.ticks.appendChild(lab);
    }
    var cap = svgEl('text', { x: (ZX0 + ZX1) / 2, y: ZY + 33, 'class': 'fc-lbl fc-lbl-mid' });
    cap.textContent = 'millimetres from the paraxial centre of curvature';
    zoomParts.ticks.appendChild(cap);

    // returning rays for a handful of zones, and their axial crossings
    var n = 0;
    for (var i = 0; i < ZONES.length; i++) {
      var idx = Math.round(ZONES[i] * NZ);
      var zc = tab.zc[idx], sl = tab.sl[idx];
      for (var s = 0; s < 2; s++) {
        var sgn = s ? -1 : 1;
        var y0 = sgn * sl * (zmin - zc), y1 = sgn * sl * (zmax - zc);
        var el = zoomParts.rays[n++];
        el.setAttribute('x1', Z(zmin)); el.setAttribute('y1', Y(y0));
        el.setAttribute('x2', Z(zmax)); el.setAttribute('y2', Y(y1));
      }
      var inWindow = zc >= zmin && zc <= zmax;
      zoomParts.dots[i].setAttribute('cx', Z(zc));
      zoomParts.dots[i].setAttribute('cy', ZY);
      zoomParts.dots[i].setAttribute('opacity', inWindow ? 1 : 0);
    }

    // the blade, occluding everything below the knife height
    var bx = Z(tab.xStage), by = Y(state.knife);
    zoomParts.blade.setAttribute('x', bx - 2);
    zoomParts.blade.setAttribute('y', by);
    zoomParts.blade.setAttribute('height', Math.max(0, ZY + ZYSPAN + 4 - by));

    var vExag = (1 / pxPerMm) / (1 / pxPerMmY);
    zoomCap.textContent = 'Horizontal scale is real millimetres. Vertical scale is exaggerated about '
      + vExag.toFixed(0) + '×. Dots mark where each zone’s light crosses the axis; '
      + 'the bar is the knife edge, riding at the stage position.';
  }

  /* ---------- panel 3: the mirror face ---------- */
  var faceCanvas = root.querySelector('#fc-face');
  var fctx = faceCanvas.getContext('2d');
  var SZ = faceCanvas.width;
  var faceImg = fctx.createImageData(SZ, SZ);

  function drawFace(tab) {
    var data = faceImg.data;
    var c = SZ / 2, pxPerMm = (SZ / 2 - 2) / geom.rEdge;
    var ys = tab.ys, rEdge = geom.rEdge;
    var lo = state.knife - geom.soft, hi = state.knife + geom.soft, span = hi - lo;
    for (var py = 0; py < SZ; py++) {
      var v = (py - c) / pxPerMm;
      for (var px = 0; px < SZ; px++) {
        var u = (px - c) / pxPerMm;
        var r = Math.sqrt(u * u + v * v);
        var o = (py * SZ + px) * 4;
        if (r > rEdge) { data[o + 3] = 0; continue; }
        var xk;
        if (r < 1e-5) {
          xk = 0;
        } else {
          var fi = r / rEdge * NZ, i0 = fi | 0;
          if (i0 >= NZ) i0 = NZ - 1;
          xk = (ys[i0] + (ys[i0 + 1] - ys[i0]) * (fi - i0)) * (u / r);
        }
        var b = (xk - lo) / span;
        if (b < 0) b = 0; else if (b > 1) b = 1;
        b = b * b * (3 - 2 * b);
        var g = (16 + b * 208) | 0;
        data[o] = g; data[o + 1] = g; data[o + 2] = g; data[o + 3] = 255;
      }
    }
    fctx.putImageData(faceImg, 0, 0);

    if (state.zones) {
      fctx.strokeStyle = 'rgba(224,169,43,0.55)';
      fctx.lineWidth = 1;
      for (var i = 0; i < ZONES.length; i++) {
        fctx.beginPath();
        fctx.arc(c, c, ZONES[i] * (SZ / 2 - 2), 0, Math.PI * 2);
        fctx.stroke();
      }
    }
  }

  /* ---------- readouts ---------- */
  var ro = {
    stage: root.querySelector('#fc-ro-stage'),
    thou: root.querySelector('#fc-ro-thou'),
    zone: root.querySelector('#fc-ro-zone'),
    total: root.querySelector('#fc-ro-total'),
    stageVal: root.querySelector('#fc-stage-val'),
    knifeVal: root.querySelector('#fc-knife-val')
  };

  function nulledZone(tab) {
    // A zone nulls exactly when its axial crossing lands on the knife plane.
    if (tab.zc[NZ] - tab.zc[0] < 0.004) {
      return Math.abs(tab.xStage - tab.zc[NZ]) < 0.004 ? 'every zone at once' : null;
    }
    for (var i = 1; i <= NZ; i++) {
      if ((tab.zc[i - 1] - tab.xStage) * (tab.zc[i] - tab.xStage) <= 0) {
        var f = (tab.xStage - tab.zc[i - 1]) / (tab.zc[i] - tab.zc[i - 1]);
        var frac = (i - 1 + f) / NZ;
        return (frac * 100).toFixed(1) + '% of radius (r = ' + (frac * geom.rEdge).toFixed(1) + ' mm)';
      }
    }
    return null;
  }

  function drawReadout(tab) {
    var s = state.stage;
    var txt = (s >= 0 ? '+' : '−') + Math.abs(s).toFixed(3) + ' mm';
    ro.stage.textContent = txt;
    ro.stageVal.textContent = txt;
    ro.thou.textContent = (s / 0.0254).toFixed(1) + ' thou';
    ro.knifeVal.textContent = state.knife.toFixed(3) + ' mm';
    var z = nulledZone(tab);
    ro.zone.textContent = z || 'none — stage is off the ends';
    ro.total.textContent = state.figure === 'sphere'
      ? '0 mm (a sphere has one)'
      : geom.edgeNull.toFixed(3) + ' mm (' + (geom.edgeNull / 0.0254).toFixed(1) + ' thou)';
  }

  /* ---------- render loop ---------- */
  var pending = false;
  function render() {
    pending = false;
    var tab = build();
    drawLayout();
    drawZoom(tab);
    drawFace(tab);
    drawReadout(tab);
  }
  function schedule() {
    if (pending) return;
    pending = true;
    window.requestAnimationFrame(render);
  }

  /* ---------- wiring ---------- */
  stageEl.addEventListener('input', function () { state.stage = sliderToStage(+stageEl.value); schedule(); });
  knifeEl.addEventListener('input', function () { state.knife = sliderToKnife(+knifeEl.value); schedule(); });

  root.querySelector('#fc-mirror').addEventListener('change', function (e) {
    setMirror(e.target.value);
    stageEl.value = stageToSlider();
    knifeEl.value = knifeToSlider();
    schedule();
  });

  root.querySelector('#fc-figure').addEventListener('click', function (e) {
    var b = e.target.closest('button');
    if (!b) return;
    state.figure = b.getAttribute('data-fig');
    // Park a sphere on its single null, so the point lands immediately.
    if (state.figure === 'sphere') { state.stage = 0; stageEl.value = stageToSlider(); }
    var btns = this.querySelectorAll('button');
    for (var i = 0; i < btns.length; i++) btns[i].classList.toggle('fc-on', btns[i] === b);
    schedule();
  });

  root.querySelector('#fc-zones').addEventListener('change', function (e) {
    state.zones = e.target.checked; schedule();
  });

  root.querySelector('.fc-presets').addEventListener('click', function (e) {
    var b = e.target.closest('button');
    if (!b) return;
    var f = parseFloat(b.getAttribute('data-zone'));
    var r = f * geom.rEdge;
    state.stage = state.figure === 'sphere' ? 0 : r * r / (2 * geom.R);
    stageEl.value = stageToSlider();
    schedule();
  });

  setMirror('6f5');
  state.stage = 0;
  stageEl.value = stageToSlider();
  knifeEl.value = knifeToSlider();
  initZoom();
  render();
})();
</script> <h2 id="the-numbers-i-could-never-find">The numbers I could never find</h2> <p>Here is the table I went looking for and never turned up. This is a 6” f/5, with the source and knife edge riding on the same stage — the ordinary way an amateur tester is built.</p> <table> <thead> <tr> <th>Zone</th> <th>30%</th> <th>50%</th> <th>70.7%</th> <th>85%</th> <th>edge</th> </tr> </thead> <tbody> <tr> <td>Radius \(r\) (mm)</td> <td>22.9</td> <td>38.1</td> <td>53.9</td> <td>64.8</td> <td>76.2</td> </tr> <tr> <td>Stage offset (mm)</td> <td>0.172</td> <td>0.476</td> <td>0.952</td> <td>1.376</td> <td><strong>1.905</strong></td> </tr> <tr> <td>Stage offset (thou)</td> <td>6.8</td> <td>18.8</td> <td>37.5</td> <td>54.2</td> <td><strong>75.0</strong></td> </tr> </tbody> </table> <p>Two things jump out. The first is how small the whole range is: the entire test happens inside seventy-five thousandths of an inch. A cheap dial indicator reads to one thou, which gets you about seventy-five discriminable steps across the mirror. That’s <em>workable</em>, and it’s exactly why dial indicators are the traditional answer — but it’s not generous.</p> <p>The second is that the spacing is not even. The offset goes as \(r^2\), so the zones bunch up badly near the centre. The first 30% of the mirror’s radius occupies less than 7 thou of travel. The last 15% occupies more than 20. This is why zonal masks put their openings at equal <em>area</em> intervals rather than equal radius intervals — the outer zones are both optically more important and easier to measure, so that’s where you spend your resolution.</p> <p>For other mirrors, the whole scale moves:</p> <table> <thead> <tr> <th>Mirror</th> <th>R (mm)</th> <th>Glass to remove at edge</th> <th>Centre-to-edge travel</th> </tr> </thead> <tbody> <tr> <td>6” f/8</td> <td>2438</td> <td>0.29 µm</td> <td>1.191 mm (46.9 thou)</td> </tr> <tr> <td>6” f/5</td> <td>1524</td> <td>1.19 µm</td> <td>1.905 mm (75.0 thou)</td> </tr> <tr> <td>8” f/6</td> <td>2438</td> <td>0.92 µm</td> <td>2.117 mm (83.3 thou)</td> </tr> </tbody> </table> <p>Fast mirrors are harder to make but <em>easier to test</em> — a shorter radius of curvature spreads the zones further apart. The f/8 is a gentler figuring job, but you’re reading it through a smaller window.</p> <h2 id="the-factor-of-two-that-wrecked-my-afternoon">The factor of two that wrecked my afternoon</h2> <p>Now the part that cost me the most time, and the reason I’m confident the tables above are worth publishing.</p> <p><strong>Published Foucault tables disagree with each other by exactly 2×, and almost none of them say which convention they’re using.</strong></p> <p>There are two ways to build a tester:</p> <ul> <li><strong>The source moves with the knife edge.</strong> Pinhole and blade on one carriage. This is how nearly every amateur tester is built, mine included.</li> <li><strong>The source stays put</strong> at the centre of curvature, and only the knife edge slides.</li> </ul> <p>These do not give the same readings, and the reason is a small piece of optics that’s easy to miss. Near the centre of curvature a mirror images with magnification −1, and the longitudinal consequence of that is a mirror image too: <strong>push the source one millimetre away from the mirror, and the returning image moves one millimetre <em>toward</em> it.</strong></p> <p>So when the source rides along with the blade, every millimetre of stage travel closes the gap between blade and image by <em>two</em> millimetres. You reach the null in half the distance.</p> \[\text{moving source: } \delta = \frac{r^2}{2R} \qquad\qquad \text{fixed source: } \delta = \frac{r^2}{R}\] <p>The classic textbook figure — “a 6-inch f/8 needs 0.094 inch of knife-edge travel” — is the <strong>fixed-source</strong> number. My ray trace puts it at 0.0938 inch, so that’s clearly where it comes from. But if you build the usual moving-source tester, bolt on a dial indicator, and go looking for 94 thou, you will be hunting for a null that is only ever going to be 47 thou away, and you will conclude that your mirror, your tester, or your arithmetic is broken.</p> <p>It’s none of the three. It’s the convention.</p> <p><em>(If you want to check me: the simulation above is a moving-source tester, because the pinhole is drawn on the carriage. That’s not decoration — the stage position it reports is the source position too, and the null it finds is the moving-source null.)</em></p> <h2 id="why-zones-instead-of-just-looking">Why zones, instead of just looking</h2> <p>If you’ve made it this far you might reasonably ask: if the shadow pattern tells you the shape, why measure anything? Why not just look at the mirror until it looks right?</p> <p>Because your eye is superb at detecting <em>whether</em> a zone is uniformly grey, and hopeless at judging <em>how grey</em> it is compared to the zone next to it. The Foucault test is a fantastic null detector and a poor photometer. So you don’t ask it “how much error is here” — you ask it “is this zone nulled <em>yet</em>,” slide the stage until the answer is yes, and write down the number.</p> <p>That’s what a <strong>Couder mask</strong> is for: a piece of cardboard over the mirror with pairs of windows cut at known radii, one pair exposed at a time. You null each pair in turn, record the stage reading, and you now have a handful of numbers that describe the surface. Compare them against the \(r^2/2R\) column, and the differences are your figure errors — which you convert to wavefront error and, eventually, to a decision about where to push on the glass next.</p> <p>One detail that looks arbitrary until it isn’t: the mask windows always sit to the <strong>left and right</strong> of the vertical knife edge, never above and below. That’s because a knife edge cutting vertically is only sensitive to ray displacement in the horizontal direction, and the windows have to be where the blade can actually read them.</p> <h2 id="where-the-automation-comes-in">Where the automation comes in</h2> <p>Everything above is done, traditionally, by a human being in a dark room, squinting at a grey disk and turning a micrometer by hand. It works — it has produced most of the good amateur telescope mirrors in existence — but it has three problems: your eye adapts and drifts, one thou of dial indicator is a coarse ruler on a 75-thou range, and a full set of zonal readings takes long enough that the mirror’s temperature changes while you’re taking them.</p> <p>All three are solvable, and none of the solutions are exotic:</p> <ul> <li><strong>Replace the eye with a camera.</strong> A sensor doesn’t adapt, and it reports actual numbers per pixel rather than an impression. You can difference two frames and see a null far more precisely than you can judge one by eye.</li> <li><strong>Replace the dial indicator with a motorised stage.</strong> A stepper on a fine-pitch screw resolves well under a micron of travel, and it can sweep the entire 1.9 mm range in seconds.</li> <li><strong>Replace the null-hunting with a fit.</strong> Once you have images across a full sweep, you don’t need to find each zone’s null by hand at all — you fit the whole measured set against a forward model of the surface and let the optimiser find the figure.</li> </ul> <p>That last point is why I wrote the simulation the way I did. The code in this page <em>is</em> a forward model: give it a surface and a stage position, and it predicts the image. Automated testing is that same function, run backwards — take the images, and solve for the surface that produced them.</p> <p>That’s the project. Follow along at <a href="/projects/3_automated_foucault/">Automated Foucault</a>, where the mirror under test is the <a href="/projects/2_atm_6in_f5/">6” f/5</a> I’m grinding.</p>]]></content><author><name></name></author><category term="automated-foucault"/><category term="optics"/><category term="telescopes"/><category term="atm"/><summary type="html"><![CDATA[Everyone explains the Foucault test with a hand-wave and a photo of a donut. Nobody tells you how far the knife edge actually moves. Here's the real geometry, the real distances, and an interactive ray-traced simulation you can drag.]]></summary></entry><entry><title type="html">Homing a Telescope Mount With Nothing But Gravity</title><link href="https://davidfobar.github.io/blog/2026/sensorless-telescope-homing/" rel="alternate" type="text/html" title="Homing a Telescope Mount With Nothing But Gravity"/><published>2026-09-16T10:00:00+00:00</published><updated>2026-09-16T10:00:00+00:00</updated><id>https://davidfobar.github.io/blog/2026/sensorless-telescope-homing</id><content type="html" xml:base="https://davidfobar.github.io/blog/2026/sensorless-telescope-homing/"><![CDATA[<p>This is a deep dive into one piece of <a href="/projects/1_celestial_sync/">CelestialSync</a>, the open-source telescope mount I’m building. It started as a class paper — <a href="/assets/img/projects/celestial_sync/CS_KalmanHoming.pdf">the full write-up is here</a> if you want the dense version with derivations and MATLAB code. This post is the version I wish I’d had going in: build the intuition first, then the math, with a couple of sliders to play with along the way.</p> <p><strong>The problem, in one sentence:</strong> the mount doesn’t know which way it’s pointing when it powers on, it has no encoders or limit switches to tell it, and yet it needs to figure that out to within a fraction of a degree using nothing but two accelerometers — sensors that only ever tell you which way is “down.”</p> <h2 id="why-the-mount-needs-to-know-where-it-is">Why the mount needs to know where it is</h2> <p>Equatorial mounts track the sky by slowly rotating one axis (Right Ascension, or RA) at exactly the sidereal rate, once every 23 hours 56 minutes. The gears that do that rotation are never perfect. CelestialSync uses a 5000:1 reduction (a planetary gearbox feeding a harmonic drive) to turn a stepper motor’s steps into smooth, slow rotation — but the harmonic drive itself has a repeatable wobble, a few arcseconds to a few tens of arcseconds, that repeats once per revolution of the drive. Left alone, that wobble is enough to smear stars into short streaks in any exposure longer than a second or two.</p> <p>The fix — Periodic Error Correction (PEC) — is simple in principle: measure the wobble once, then play back a compensating correction every time you track. The catch is that the correction table is indexed by <em>where the gear train currently is in its cycle</em>. If the mount doesn’t know its gear phase at startup, it applies the correction at the wrong point in the cycle and makes tracking worse, not better.</p> <p>So before anything else, the mount needs to find “home” — a precise, repeatable reference position — every time it’s powered on. Off-the-shelf solutions use absolute encoders or homing switches for this. CelestialSync doesn’t have either. Instead, it has two small accelerometers, one bolted to each axis, and a filter that turns their readings into a position estimate.</p> <h2 id="what-is-a-kalman-filter-really">What is a Kalman filter, really?</h2> <p>Set the telescope mount aside for a second. Imagine you’re walking across a dark room, blindfolded, trying to find a specific spot. You have two sources of information, and neither one is good enough alone:</p> <ul> <li><strong>You can count your steps.</strong> You know roughly how big your strides are, so you can keep a running estimate of where you are. But small errors creep in with every step — you drift a little left, a stride is a bit short — and after enough steps that estimate is worthless on its own.</li> <li><strong>You can occasionally reach out and touch something</strong> — a wall, a piece of furniture you recognize. That gives you a fix on your true position, but it’s noisy (your hand isn’t perfectly precise) and it doesn’t tell you anything about which <em>direction</em> you’re facing between touches.</li> </ul> <p>A Kalman filter is the formal version of doing both at once, and doing it well. At every step it:</p> <ol> <li><strong>Predicts</strong> where you should be now, based on where you were and what you just did (your stride count).</li> <li><strong>Measures</strong> something about the world (your hand touches a wall).</li> <li><strong>Corrects</strong> its prediction toward the measurement — but not all the way. It weighs the prediction and the measurement by how much it trusts each one, and blends them. If your steps have been very consistent, it leans on the prediction. If the wall-touch was clean, it leans on that instead.</li> </ol> <p>The genuinely useful part is that the filter also tracks <em>how confident it is</em>, and that confidence shrinks over time as evidence accumulates — it’s not just a single guess, it’s a guess plus a shrinking error bar. The “<strong>E</strong>xtended” in Extended Kalman Filter just means the same predict/measure/correct loop, but adapted for cases where the relationship between your position and what you measure isn’t a straight line — which, for a rotating accelerometer, it very much isn’t. Instead of a simple weighted average, the EKF re-linearizes (takes a local straight-line approximation) around the current best guess at every step, using calculus to figure out the right blend.</p> <p>For CelestialSync: the “stride count” is the known number of motor steps commanded during a slew (we know this exactly — we’re the one telling the stepper motor what to do). The “wall touch” is the accelerometer reading. Neither one alone can find home. Together, they can.</p> <h2 id="what-the-accelerometers-actually-measure">What the accelerometers actually measure</h2> <p>An accelerometer sitting still on a table isn’t measuring “nothing” — it’s measuring the force needed to hold it up against gravity, which is indistinguishable from measuring a constant ~9.81 m/s² pointing “up,” in whatever direction “up” is <em>relative to the sensor’s own body</em>.</p> <p>That last part is the whole trick. Gravity itself never changes. But as you rotate the sensor, the same fixed gravity vector gets projected differently onto the sensor’s own x, y, and z axes. Tip the sensor 90°, and a component that used to read zero now reads the full 9.81 m/s², and vice versa. The relationship between <em>orientation</em> and <em>what the sensor reports</em> is completely deterministic — pure trigonometry — provided you know how the sensor is mounted.</p> <p>That means the reverse is also (in principle) true: read the three numbers off the accelerometer, and you can work backward to the sensor’s orientation.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/sensorless-telescope-homing/CS_axes_annotated-480.webp 480w,/assets/img/posts/sensorless-telescope-homing/CS_axes_annotated-800.webp 800w,/assets/img/posts/sensorless-telescope-homing/CS_axes_annotated-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/sensorless-telescope-homing/CS_axes_annotated.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">The two accelerometers and their body-frame axes. The RA sensor rotates with the RA stage; the DEC sensor rides on top of it and rotates with the DEC stage.</figcaption> </figure> <p>CelestialSync has one accelerometer rigidly fixed to the RA stage, and a second fixed to the DEC stage — which itself sits on top of, and rotates with, the RA stage. That nesting matters:</p> <ul> <li>The <strong>RA accelerometer</strong>’s reading depends only on the RA angle, \(\theta_{RA}\).</li> <li>The <strong>DEC accelerometer</strong>’s reading depends on <em>both</em> \(\theta_{RA}\) and \(\theta_{DEC}\), because whatever the RA stage is doing gets carried along before the DEC stage’s own rotation is even applied.</li> </ul> <p>Working out what a sensor <em>should</em> read, given a candidate orientation, means chaining together every rotation and fixed mounting offset between “gravity in the real world” and “gravity as this specific chip’s x/y/z axes see it.” Get one of those transforms wrong — or apply them in the wrong order — and the predicted reading is wrong. That chaining is the “coordinate frame transformation” problem, and it’s worth building intuition for before touching the filter itself.</p> <h2 id="try-it-watch-gravity-move-through-the-coordinate-chain">Try it: watch gravity move through the coordinate chain</h2> <p>The two sliders below control the RA and DEC angles directly — exactly the two unknowns the real filter is trying to estimate. Everything else (the mount’s latitude tilt, the sensors’ fixed mounting offsets) is held constant, using the same 40°-altitude homing configuration the paper ultimately recommends. Each dial shows the accelerometer’s own body-frame axes as a small “stage mark” (where the sensor is physically pointing) and the live gravity vector it measures, projected onto that sensor’s x/y plane. The numbers underneath are the actual \((g_x, g_y, g_z)\) components that sensor would report right now, in m/s².</p> <div class="cs-sim" id="cs-homing-sim"> <div class="cs-sim-controls"> <div class="cs-slider-row"> <label for="cs-ra-slider">RA angle <span class="cs-axis-tag cs-tag-ra">θ<sub>RA</sub></span></label> <input type="range" id="cs-ra-slider" min="0" max="360" step="1" value="30"/> <span class="cs-slider-value" id="cs-ra-value">30°</span> </div> <div class="cs-slider-row"> <label for="cs-dec-slider">DEC angle <span class="cs-axis-tag cs-tag-dec">θ<sub>DEC</sub></span></label> <input type="range" id="cs-dec-slider" min="-90" max="90" step="1" value="20"/> <span class="cs-slider-value" id="cs-dec-value">20°</span> </div> <button type="button" id="cs-home-btn">Snap to home position (0°, 0°)</button> </div> <div class="cs-dials"> <div class="cs-dial-panel"> <h4>RA accelerometer</h4> <svg viewBox="0 0 200 200" class="cs-dial-svg"> <defs> <marker id="cs-arrow-ra" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse"> <path d="M0,0 L10,5 L0,10 z" class="cs-arrowhead"></path> </marker> </defs> <circle cx="100" cy="100" r="90" class="cs-dial-circle"></circle> <line x1="100" y1="10" x2="100" y2="190" class="cs-dial-gridline"></line> <line x1="10" y1="100" x2="190" y2="100" class="cs-dial-gridline"></line> <g id="cs-ra-stage-mark"> <line x1="100" y1="100" x2="100" y2="25" class="cs-stage-mark"></line> </g> <line id="cs-ra-arrow" x1="100" y1="100" x2="100" y2="30" class="cs-gravity-arrow" marker-end="url(#cs-arrow-ra)"></line> <circle cx="100" cy="100" r="4" class="cs-dial-center"></circle> </svg> <div class="cs-readout"> <div><span>g<sub>x</sub></span><b id="cs-ra-gx">0.00</b></div> <div><span>g<sub>y</sub></span><b id="cs-ra-gy">0.00</b></div> <div><span>g<sub>z</sub></span><b id="cs-ra-gz">0.00</b></div> </div> </div> <div class="cs-dial-panel"> <h4>DEC accelerometer</h4> <svg viewBox="0 0 200 200" class="cs-dial-svg"> <defs> <marker id="cs-arrow-dec" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse"> <path d="M0,0 L10,5 L0,10 z" class="cs-arrowhead"></path> </marker> </defs> <circle cx="100" cy="100" r="90" class="cs-dial-circle"></circle> <line x1="100" y1="10" x2="100" y2="190" class="cs-dial-gridline"></line> <line x1="10" y1="100" x2="190" y2="100" class="cs-dial-gridline"></line> <g id="cs-dec-stage-mark"> <line x1="100" y1="100" x2="100" y2="25" class="cs-stage-mark cs-stage-mark-dec"></line> </g> <line id="cs-dec-arrow" x1="100" y1="100" x2="100" y2="30" class="cs-gravity-arrow cs-gravity-arrow-dec" marker-end="url(#cs-arrow-dec)"></line> <circle cx="100" cy="100" r="4" class="cs-dial-center"></circle> </svg> <div class="cs-readout"> <div><span>g<sub>x</sub></span><b id="cs-dec-gx">0.00</b></div> <div><span>g<sub>y</sub></span><b id="cs-dec-gy">0.00</b></div> <div><span>g<sub>z</sub></span><b id="cs-dec-gz">0.00</b></div> </div> </div> </div> <p class="cs-sim-note">The thin colored tick on each dial is the sensor's own physical orientation — where the stage has actually rotated to. The arrow is what gravity looks like <em>from that sensor's point of view</em> right now. Try moving only the RA slider: the DEC dial's arrow and numbers move too, even though the DEC slider never changed. That's the coupling the paper calls "the coupled rotation chain" — and it's why the filter has to estimate both angles jointly. Home is defined as the specific condition $$h_{RA,y}=0$$ and $$h_{DEC,z}=0$$ simultaneously — hit the button above to see exactly what that configuration looks like on both dials at once.</p> </div> <style>
  #cs-homing-sim.cs-sim {
    border: 1px solid var(--global-divider-color);
    border-radius: 12px;
    padding: 1.25rem 1.5rem 1.5rem;
    margin: 2rem 0;
    background: var(--global-card-bg-color);
  }
  #cs-homing-sim .cs-sim-controls {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    margin-bottom: 1.25rem;
  }
  #cs-homing-sim .cs-slider-row {
    display: grid;
    grid-template-columns: 9rem 1fr 3.5rem;
    align-items: center;
    gap: 0.75rem;
  }
  #cs-homing-sim .cs-slider-row label {
    font-size: 0.9rem;
    color: var(--global-text-color);
  }
  #cs-homing-sim input[type="range"] {
    width: 100%;
  }
  #cs-homing-sim .cs-slider-value {
    font-variant-numeric: tabular-nums;
    text-align: right;
    font-size: 0.9rem;
    color: var(--global-text-color);
  }
  #cs-homing-sim .cs-axis-tag {
    font-size: 0.75rem;
    padding: 0.05rem 0.4rem;
    border-radius: 6px;
    margin-left: 0.25rem;
  }
  #cs-homing-sim .cs-tag-ra {
    background: rgba(124, 58, 237, 0.15);
    color: #7c3aed;
  }
  #cs-homing-sim .cs-tag-dec {
    background: rgba(234, 88, 12, 0.15);
    color: #ea580c;
  }
  #cs-homing-sim #cs-home-btn {
    align-self: flex-start;
    margin-top: 0.25rem;
    padding: 0.4rem 0.9rem;
    font-size: 0.85rem;
    border-radius: 8px;
    border: 1px solid var(--global-theme-color);
    background: transparent;
    color: var(--global-theme-color);
    cursor: pointer;
  }
  #cs-homing-sim #cs-home-btn:hover {
    background: var(--global-theme-color);
    color: var(--global-card-bg-color);
  }
  #cs-homing-sim .cs-dials {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
    justify-content: center;
  }
  #cs-homing-sim .cs-dial-panel {
    flex: 1 1 220px;
    max-width: 260px;
    text-align: center;
  }
  #cs-homing-sim .cs-dial-panel h4 {
    margin: 0 0 0.5rem;
    font-size: 0.95rem;
    color: var(--global-text-color);
  }
  #cs-homing-sim .cs-dial-svg {
    width: 100%;
    height: auto;
  }
  #cs-homing-sim .cs-dial-circle {
    fill: none;
    stroke: var(--global-divider-color);
    stroke-width: 2;
  }
  #cs-homing-sim .cs-dial-gridline {
    stroke: var(--global-divider-color);
    stroke-width: 1;
    opacity: 0.5;
  }
  #cs-homing-sim .cs-dial-center {
    fill: var(--global-text-color);
  }
  #cs-homing-sim .cs-stage-mark {
    stroke: #7c3aed;
    stroke-width: 3;
    opacity: 0.55;
  }
  #cs-homing-sim .cs-stage-mark-dec {
    stroke: #ea580c;
  }
  #cs-homing-sim .cs-gravity-arrow {
    stroke: var(--global-text-color);
    stroke-width: 3;
  }
  #cs-homing-sim .cs-arrowhead {
    fill: var(--global-text-color);
  }
  #cs-homing-sim .cs-readout {
    display: flex;
    justify-content: center;
    gap: 0.9rem;
    margin-top: 0.6rem;
    font-size: 0.85rem;
  }
  #cs-homing-sim .cs-readout div {
    display: flex;
    flex-direction: column;
    align-items: center;
    color: var(--global-text-color);
  }
  #cs-homing-sim .cs-readout span {
    opacity: 0.6;
    font-size: 0.75rem;
  }
  #cs-homing-sim .cs-readout b {
    font-variant-numeric: tabular-nums;
  }
  #cs-homing-sim .cs-sim-note {
    font-size: 0.85rem;
    opacity: 0.85;
    margin: 1.1rem 0 0;
    line-height: 1.5;
  }
  @media (max-width: 600px) {
    #cs-homing-sim .cs-slider-row {
      grid-template-columns: 5.5rem 1fr 3.2rem;
    }
    #cs-homing-sim .cs-dials {
      flex-direction: column;
      align-items: center;
    }
  }
</style> <script>
(function () {
  var G = 9.81;
  var LAT_DEG = 40; // the paper's recommended fixed-altitude homing configuration

  function deg2rad(d) { return (d * Math.PI) / 180; }

  function matVec(m, v) {
    return [
      m[0][0] * v[0] + m[0][1] * v[1] + m[0][2] * v[2],
      m[1][0] * v[0] + m[1][1] * v[1] + m[1][2] * v[2],
      m[2][0] * v[0] + m[2][1] * v[1] + m[2][2] * v[2],
    ];
  }

  function rRA(thetaDeg) {
    var t = deg2rad(thetaDeg), c = Math.cos(t), s = Math.sin(t);
    return [[c, -s, 0], [s, c, 0], [0, 0, 1]];
  }

  function rDEC(thetaDeg) {
    var t = deg2rad(thetaDeg), c = Math.cos(t), s = Math.sin(t);
    return [[1, 0, 0], [0, c, -s], [0, s, c]];
  }

  var T_DEC = [[-1, 0, 0], [0, 0, -1], [0, 1, 0]];

  function gPolar() {
    var lat = deg2rad(LAT_DEG);
    return [G * Math.cos(lat), 0, -G * Math.sin(lat)];
  }

  function computeReadings(thetaRA, thetaDEC) {
    var gp = gPolar();
    var gRA = matVec(rRA(thetaRA), gp);
    var gDEC = matVec(T_DEC, matVec(rDEC(thetaDEC), gRA));
    return { gRA: gRA, gDEC: gDEC };
  }

  var root = document.getElementById('cs-homing-sim');
  if (!root) return;

  var raSlider = root.querySelector('#cs-ra-slider');
  var decSlider = root.querySelector('#cs-dec-slider');
  var raValue = root.querySelector('#cs-ra-value');
  var decValue = root.querySelector('#cs-dec-value');
  var raStageMark = root.querySelector('#cs-ra-stage-mark');
  var decStageMark = root.querySelector('#cs-dec-stage-mark');
  var raArrow = root.querySelector('#cs-ra-arrow');
  var decArrow = root.querySelector('#cs-dec-arrow');
  var homeBtn = root.querySelector('#cs-home-btn');

  var readoutEls = {
    raGx: root.querySelector('#cs-ra-gx'),
    raGy: root.querySelector('#cs-ra-gy'),
    raGz: root.querySelector('#cs-ra-gz'),
    decGx: root.querySelector('#cs-dec-gx'),
    decGy: root.querySelector('#cs-dec-gy'),
    decGz: root.querySelector('#cs-dec-gz'),
  };

  var SCALE = 70 / G;

  function setArrow(el, gx, gy) {
    el.setAttribute('x2', (100 + gx * SCALE).toFixed(2));
    el.setAttribute('y2', (100 - gy * SCALE).toFixed(2));
  }

  function update() {
    var thetaRA = parseFloat(raSlider.value);
    var thetaDEC = parseFloat(decSlider.value);

    raValue.textContent = thetaRA.toFixed(0) + '°';
    decValue.textContent = thetaDEC.toFixed(0) + '°';

    raStageMark.setAttribute('transform', 'rotate(' + (-thetaRA) + ' 100 100)');
    decStageMark.setAttribute('transform', 'rotate(' + (-thetaDEC) + ' 100 100)');

    var r = computeReadings(thetaRA, thetaDEC);

    setArrow(raArrow, r.gRA[0], r.gRA[1]);
    setArrow(decArrow, r.gDEC[0], r.gDEC[1]);

    readoutEls.raGx.textContent = r.gRA[0].toFixed(2);
    readoutEls.raGy.textContent = r.gRA[1].toFixed(2);
    readoutEls.raGz.textContent = r.gRA[2].toFixed(2);
    readoutEls.decGx.textContent = r.gDEC[0].toFixed(2);
    readoutEls.decGy.textContent = r.gDEC[1].toFixed(2);
    readoutEls.decGz.textContent = r.gDEC[2].toFixed(2);
  }

  raSlider.addEventListener('input', update);
  decSlider.addEventListener('input', update);
  homeBtn.addEventListener('click', function () {
    raSlider.value = 0;
    decSlider.value = 0;
    update();
  });

  update();
})();
</script> <h2 id="from-intuition-to-an-estimator-building-the-ekf">From intuition to an estimator: building the EKF</h2> <p>The sliders above compute a reading from a <em>known</em> orientation — the easy direction. The filter has to run that process in reverse, recover an <em>unknown</em> orientation from noisy readings, and it can’t just invert the trig equations sample-by-sample. Three things get in the way:</p> <ul> <li><strong>The sensors have their own constant bias</strong> — a small, unknown offset baked into each axis of each chip, on the order of ±0.1 m/s². Ignore it and you get a confidently wrong answer.</li> <li><strong>Every reading is noisy.</strong> A single sample is a bad basis for a decision that needs to land within a fraction of a degree.</li> <li><strong>The relationship between angle and reading isn’t unique in every instant</strong> — but it <em>becomes</em> unique once you fuse many samples gathered while the mount is actually turning through a known trajectory.</li> </ul> <p>That last point is why the mount doesn’t just take one reading at startup — it performs a slow, controlled slew (driven at a known, commanded rate) while continuously sampling both accelerometers, and lets the filter fuse the entire sequence.</p> <h3 id="the-state">The state</h3> <p>The filter tracks eight numbers at once:</p> \[\boldsymbol{x} = \begin{bmatrix} \theta_{RA} \\ \theta_{DEC} \\ \boldsymbol{b}_{RA} \\ \boldsymbol{b}_{DEC} \end{bmatrix} \in \mathbb{R}^8\] <p>— the two angles we actually care about, plus the three-axis bias vector for each accelerometer (six more unknowns). All eight have to be estimated together, because a biased sensor and a mis-estimated angle can look similar in a single reading; only by watching the readings evolve correctly over a whole slew can the filter tell them apart.</p> <h3 id="predict-then-correct">Predict, then correct</h3> <p>The loop is the same predict/measure/correct cycle from the walking analogy, applied at every accelerometer sample:</p> <ul> <li><strong>Predict:</strong> advance each angle by the commanded step increment since the last sample (this part is linear and exact — we know precisely how many microsteps we told the motor to take).</li> <li><strong>Measure:</strong> read the six accelerometer channels (three axes × two sensors).</li> <li><strong>Linearize:</strong> compute the Jacobian — the local slope of “predicted reading” with respect to each of the eight states — at the current estimate. This is the “Extended” step: rather than solving the true nonlinear equations, the filter uses their best straight-line approximation right where it currently thinks it is.</li> <li><strong>Correct:</strong> compare the actual reading to the predicted one, and nudge the state estimate toward the actual reading, weighted by the Kalman gain — how much the filter currently trusts the measurement versus its own prediction.</li> </ul> <h3 id="why-two-passes">Why two passes</h3> <p>Here’s a wrinkle that only shows up once you look at how <em>fast</em> different parts of the state converge. The six bias terms and the RA angle become well-determined only after a good chunk of the slew has gone by, while the DEC angle sharpens quickly. Run the filter once, forward, and by the time it reaches the end of the data the bias estimates are solid — but the angle estimate at that final sample still carries leftover uncertainty from earlier in the run.</p> <p>The fix is to run the filter twice:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/sensorless-telescope-homing/CS_fig_two_pass_ekf-480.webp 480w,/assets/img/posts/sensorless-telescope-homing/CS_fig_two_pass_ekf-800.webp 800w,/assets/img/posts/sensorless-telescope-homing/CS_fig_two_pass_ekf-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/sensorless-telescope-homing/CS_fig_two_pass_ekf.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">The two-pass architecture: a forward pass nails down the accelerometer biases, then a backward pass — with biases frozen — sharpens the angle estimate right up to the home position.</figcaption> </figure> <p><strong>Pass 1</strong> runs forward through the whole slew as an 8-state filter, estimating both angles and all six biases together. By the end, the bias estimates have converged.</p> <p><strong>Pass 2</strong> freezes those bias values and restarts as a leaner 2-state filter — just the two angles — sweeping <em>backward</em> through the same measurement sequence, from the end of the slew back to the start. Running backward means uncertainty keeps shrinking as the filter approaches sample zero, which is exactly where the home position needs to be extracted. By construction, that’s the point in the entire dataset where the filter has the most accumulated evidence behind it.</p> <h2 id="does-it-actually-work-results-from-the-paper">Does it actually work? Results from the paper</h2> <p>Before trusting any of this, the measurement model itself needs validating against real hardware — not just simulation.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/sensorless-telescope-homing/CS_fig_sensor_validation-480.webp 480w,/assets/img/posts/sensorless-telescope-homing/CS_fig_sensor_validation-800.webp 800w,/assets/img/posts/sensorless-telescope-homing/CS_fig_sensor_validation-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/sensorless-telescope-homing/CS_fig_sensor_validation.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Experimental accelerometer data (black) against the simulated model (blue) and noiseless true trajectory (red dashed), for a real homing sweep on the physical mount. The DEC channels visibly track both axes' motion — direct evidence of the coupling from the simulator above.</figcaption> </figure> <p>The real sensors track the model closely, including the DEC channel’s dependence on both angles — the same coupling the sliders above make interactive. That agreement is what justifies trusting the simulation-based results that follow.</p> <p>With the model validated, Pass 1 converges the six bias states within roughly 140° of simulated rotation:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/sensorless-telescope-homing/CS_fig_bias_convergence-480.webp 480w,/assets/img/posts/sensorless-telescope-homing/CS_fig_bias_convergence-800.webp 800w,/assets/img/posts/sensorless-telescope-homing/CS_fig_bias_convergence-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/sensorless-telescope-homing/CS_fig_bias_convergence.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Pass 1: all six accelerometer bias states converge to within their ±2σ bounds well before the end of the slew.</figcaption> </figure> <p>Pass 2 then sharpens both angle estimates as it sweeps backward toward the home position at sample zero:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/sensorless-telescope-homing/CS_fig_angle_convergence-480.webp 480w,/assets/img/posts/sensorless-telescope-homing/CS_fig_angle_convergence-800.webp 800w,/assets/img/posts/sensorless-telescope-homing/CS_fig_angle_convergence-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/sensorless-telescope-homing/CS_fig_angle_convergence.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Pass 2: angle estimates (blue) track the true trajectory (red) closely, with 1σ uncertainty (right axis, log scale) shrinking steadily as the backward pass approaches the home extraction point.</figcaption> </figure> <p>At the extraction point, 1σ uncertainty lands around 0.01°–0.03° — well inside the ±180° (±25,600 microstep) homing budget the low-cost rotary encoder needs to lock onto a precise gear phase.</p> <p>The real test is whether this holds up over many independent trials, not just one representative run. Over 100 Monte Carlo trials at two site latitudes:</p> <table> <thead> <tr> <th> </th> <th>λ = 52° RA</th> <th>λ = 52° DEC</th> <th>λ = 40° RA</th> <th>λ = 40° DEC</th> </tr> </thead> <tbody> <tr> <td>Mean error (microsteps)</td> <td>−1,807</td> <td>382</td> <td>−1,505</td> <td>317</td> </tr> <tr> <td>Std deviation (microsteps)</td> <td>14,555</td> <td>7,177</td> <td>11,370</td> <td>7,792</td> </tr> <tr> <td>RMSE (microsteps)</td> <td>14,594</td> <td>7,151</td> <td>11,413</td> <td>7,759</td> </tr> <tr> <td>Max abs error (microsteps)</td> <td>38,948</td> <td>17,392</td> <td>28,115</td> <td>19,867</td> </tr> <tr> <td><strong>Pass rate</strong></td> <td><strong>92%</strong></td> <td><strong>100%</strong></td> <td><strong>99%</strong></td> <td><strong>100%</strong></td> </tr> </tbody> </table> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/sensorless-telescope-homing/CS_fig_homing_error_histograms-480.webp 480w,/assets/img/posts/sensorless-telescope-homing/CS_fig_homing_error_histograms-800.webp 800w,/assets/img/posts/sensorless-telescope-homing/CS_fig_homing_error_histograms-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/sensorless-telescope-homing/CS_fig_homing_error_histograms.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> <figcaption class="caption">Homing error distributions over 100 Monte Carlo trials. Dashed red lines mark the ±25,600-microstep pass/fail boundary. DEC comfortably clears it at both latitudes; RA is the tighter margin, and improves at the lower latitude.</figcaption> </figure> <p>DEC passes essentially every time, at both latitudes. RA is the limiting axis — its observability (how strongly its angle actually shows up in the accelerometer readings) depends on site latitude, and gets weaker the more polar-aligned the mount is. That’s exactly why the interactive sliders above default to a 40° altitude rather than a polar-aligned angle: tilting the mount to a fixed 40° altitude for the homing routine — decoupled from wherever true polar alignment happens to be for that location — keeps the RA accelerometer’s gravity projection strong and consistent, independent of where on Earth the mount is set up. That single change took the RA pass rate from 92% to 99%.</p> <p>A 99% pass rate is good enough for how this gets used in practice: homing only has to happen once per power-on, to seed the PEC table, and the absolute position can then be retained across a session (with periodic EEPROM writes so an ungraceful shutdown doesn’t lose it entirely).</p> <h2 id="whats-next">What’s next</h2> <p>The filter above is validated in simulation and against real accelerometer data, but it hasn’t run closed-loop on the physical mount yet. That’s next: swap the simulated step counter for real stepper-pulse timestamps, and confirm the 40°-altitude homing configuration actually delivers on the pass-rate improvement outside of Monte Carlo.</p> <p>Follow the build on the <a href="/projects/1_celestial_sync/">CelestialSync project page</a>, or check out the code on <a href="https://github.com/davidfobar/CelestialSync">GitHub</a>.</p>]]></content><author><name></name></author><category term="celestial-sync"/><category term="kalman-filter"/><category term="robotics"/><category term="astronomy"/><summary type="html"><![CDATA[How two accelerometers and an Extended Kalman Filter let an equatorial mount find its way home without encoders or limit switches — walked through gradually, with an interactive simulation.]]></summary></entry></feed>